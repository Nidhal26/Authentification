<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">SymLivres</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}"><i class="fa-solid fa-house">Home</i>
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <!--parcourir une liste de category repository-->
                {% for cat in categoryRepository.findAll() %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('Livre_By_categorie',{'id':cat.id}) }}">
                        {{ cat.libelle }}</a>
                </li>
                {% endfor %}
            </ul>

            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href=""><i class="fa-solid fa-cart-shopping">Panier</i>
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_logout') }}"><i class="fa-solid fa-right-from-bracket">logout</i>
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_register') }}"><i class="fa-sharp fa-solid fa-user-plus"></i>
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_login') }}"><i class="fa-solid fa-right-to-bracket">login</i>
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                </ul>
                {% endif %}

                
            <form class="d-flex">
                <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>